[build]
  base = "web"
  command = "npm run build"
  publish = "dist"

[build.environment]
  # web/package.json requires Node >=22.12.0 (Astro 7) — pinned so Netlify's
  # build image doesn't default to an older LTS that fails the build.
  NODE_VERSION = "22.12.0"
